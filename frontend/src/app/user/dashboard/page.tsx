export default function RegisteredUserDashboard() {
  return (
    <p>
      This is the protected registered user dashboard restricted to users with the `admin` or `user`
      role.
    </p>
  );
}
